using UnityEngine;

public class HelpeeUI : MonoBehaviour
{
    public Sprite icon;
    public Sprite iconGray;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //
    }

    // Update is called once per frame
    void Update()
    {
        //
    }
}
